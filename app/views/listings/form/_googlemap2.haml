#listing_map_canvas2.jsmap

- content_for :map_js do
  :javascript
    $(document).ready(function() {
      var source = "listing_from_address";
      googlemapMarkerInit('listing_map_canvas2',"listing_from_check_address_attributes","listing_from_address",true, #{tribe_latitude}, #{tribe_longitude});
    });

- if run_js_immediately
  = yield :map_js
- else
  - content_for :extra_javascript do
    = yield :map_js
